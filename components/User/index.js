import styled from "styled-components";

const User = () => {
    return (
        <div>
            Enter
        </div>
    );
}

export default User;

const Container = styled.div`
    display: flex;
    padding: 1rem;
    :hover {
        background-color: #e9eaeb;
    }
`